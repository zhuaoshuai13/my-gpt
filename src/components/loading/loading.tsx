import ReactLoading from "react-loading";

const Loading = () => {
  return (
    <ReactLoading
      type="cubes"
      color="rgb(100, 26, 230)"
      height={20}
      width={100}
    />
  );
};

export default Loading;
